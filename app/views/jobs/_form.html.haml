= simple_form_for(@job, html: {class: 'form-horizonta;'}) do |f|
	= f.input :title, label: "Job Title"
	= f.input :description, label: "Your Description"
	= f.input :company, label: "Your Company"
	= f.input :url, label: "Link to Job"
	%br/
	= f.button :submit